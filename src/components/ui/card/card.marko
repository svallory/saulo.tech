import { cn } from "../../../lib/utils.ts";
static const styles = {
  root: "ring-foreground/10 bg-card text-card-foreground gap-(--card-spacing) overflow-hidden rounded-xl py-(--card-spacing) text-sm shadow-xs ring-1 [--card-spacing:--spacing(6)] has-[>img:first-child]:pt-0 data-[size=sm]:[--card-spacing:--spacing(4)] *:[img:first-child]:rounded-t-xl *:[img:last-child]:rounded-b-xl group/card flex flex-col",
} as const;

export interface Input extends Marko.Input<"div"> {
  size?: "default" | "sm";
}

<const/{ class: className, content, size = "default", ...rest }=input/>

<div
  data-slot="card"
  data-size=size
  class=cn(styles.root, className)
  ...rest
>
  <${content}/>
</div>
